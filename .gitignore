```json
{
  "id": "case-003",
  "title": "User Authentication Flow",
  "description": "Test case for user authentication process including login, session management, and logout functionality",
  "steps": [
    {
      "id": 1,
      "action": "Navigate to login page",
      "expected": "Login form is displayed"
    },
    {
      "id": 2,
      "action": "Enter valid credentials",
      "expected": "User is authenticated successfully"
    },
    {
      "id": 3,
      "action": "Verify session persistence",
      "expected": "Session remains active across page navigations"
    },
    {
      "id": 4,
      "action": "Logout from application",
      "expected": "User is logged out and redirected to home page"
    }
  ],
  "priority": "high",
  "tags": ["authentication", "security", "session"]
}
```