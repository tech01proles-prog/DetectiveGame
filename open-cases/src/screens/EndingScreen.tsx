import {CheckCircle2,RotateCcw,XCircle} from 'lucide-react';import {characters} from '@/game/data';import type {GameState} from '@/game/types';
export default function EndingScreen({state,onRestart}:{state:GameState;onRestart:()=>void}){const score=state.score||0;const good=score>=70;const leah=characters.find(c=>c.id==='leah')!;return <main className="ending"><div className="ending-inner"><div className="eyebrow">ДЕЛО ЗАКРЫТО</div><div className={`score-ring ${good?'good':''}`}><strong>{score}</strong><span>из 100</span></div><h1>{good?'Вы восстановили цепочку событий.':'Расследование осталось неполным.'}</h1><p>{good?'Майя была намеренно заманена в Northline Storage. Главная организаторка — Лия Моррис, которая пыталась скрыть источник материалов и получить контроль над доказательствами.':'Вашей команде не хватило нескольких ключевых связей. Можно начать заново и проверить другие направления.'}</p><div className="answer-grid"><div><span>Организатор</span><b>{leah.name}</b>{state.finalAnswers?.person==='leah'?<CheckCircle2/>:<XCircle/>}</div><div><span>Мотив</span><b>Скрыть историю с сериями RB-14</b>{state.finalAnswers?.motive==='exposure'?<CheckCircle2/>:<XCircle/>}</div><div><span>Способ</span><b>Заманивание под видом встречи</b>{state.finalAnswers?.method==='lured'?<CheckCircle2/>:<XCircle/>}</div><div><span>Ключевая точка</span><b>Northline Storage, секция B</b>{state.finalAnswers?.location==='northline_storage'?<CheckCircle2/>:<XCircle/>}</div></div><button className="btn primary" onClick={onRestart}><RotateCcw size={18}/> Новое расследование</button></div></main>}
